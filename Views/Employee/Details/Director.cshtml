@using StaffAccounting.Models.Company;
@model Director

<h1>Директор</h1>

@await Html.PartialAsync("Details/Employee", Model)

<div>
    <dl class="dl-horizontal">
        <dt>Бухгалтера</dt>
        <dd>
            <div class="items-collection">
                @{
                    int accountantsCount = Model.Accountants.Count();
                }
                @for (int accountantNumber = 0; accountantNumber < Math.Min(accountantsCount, 3); accountantNumber++)
                {
                    <a asp-action="Details" asp-controller="Employee" asp-route-id="@Model.Accountants[accountantNumber].Id">
                        @(accountantNumber+1). @Model.Accountants[accountantNumber].FullName
                    </a>
                    <br />
                }
                @if (accountantsCount > 3)
                {
                    <text>...</text>
                    <br />
                }
                <a asp-action="Index" asp-controller="Employee" asp-route-DirectorId="@Model.Id">
                    Подробнее
                </a>
            </div>
        </dd>
        <dt>Главы департамента</dt>
        <dd>
            <div class="items-collection">
                @{
                    int headsCount = Model.DepartamentHeads.Count();
                }
                @for (int headsNumber = 0; headsNumber < Math.Min(headsCount, 3); headsNumber++)
                {
                    <a asp-action="Details" asp-controller="Employee" asp-route-id="@Model.DepartamentHeads[headsNumber].Id">
                        @(headsNumber+1). @Model.DepartamentHeads[headsNumber].FullName
                    </a>
                    <br />
                }
                @if (headsCount > 3)
                {
                    <text>...</text>
                    <br />
                }
                <a asp-action="Index" asp-controller="Employee" asp-route-DirectorId="@Model.Id">
                    Подробнее
                </a>
            </div>
        </dd>
    </dl>

    <a asp-action="Edit" asp-controller="Employee" asp-route-id="@Model.Id" class="btn btn-outline-secondary btn-sm">Изменить</a>

    <form asp-action="Delete" asp-controller="Employee" asp-route-id="@Model.Id">
        <input type="submit" value="Удалить" class="btn btn-outline-danger btn-sm" />
    </form>
</div>