@model StaffAccounting.Models.Company.Employee
@{
    ViewBag.Title = Model.FullName;
}

<h2>@Model.FirstName</h2>
<div>
    <dl class="dl-horizontal">
        <dt>Идентификатор</dt>
        <dd>@Model.Id</dd>
 
        <dt>Возраст</dt>
        <dd>@Model.Birthday.Year</dd>

        <dt>Пол</dt>
        <dd>@Model.Sex</dd>
    </dl>
</div>